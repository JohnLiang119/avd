## MODIFIED Requirements

### Requirement: 時間錨點的推進邊界

頻道的時間錨點（`lastPublishedTime`）MUST NOT（不得）推進超過本次檢查中任何「未處理影片」的發布時間。未處理影片 MUST（必須）定義為「已通過該頻道關鍵字篩選、但其**直播狀態無法判定**而未建立下載任務者」；此類影片 MUST（必須）保持在錨點之後，使其於後續檢查中重新被評估。

**已判定為直播中或排程未開播的影片 MUST NOT（不得）被列為未處理影片**，錨點得以越過它們。此類影片與未命中關鍵字篩選的影片同列 —— 兩者皆為系統已知且明確不予下載的項目。以壓住錨點來表達「這支之後還要再看」，在**永遠存在排程直播的頻道**上會使錨點永久卡死：每日固定節目會提前建立排程直播項目，feed 中因此永遠有未處理影片，錨點永遠追不上，每輪把錨點之後的所有影片重新判定為新片。該現象僅靠「已在下載佇列中」的去重掩蓋，使用者一旦清空佇列即全部湧入。

**未命中關鍵字篩選的影片同樣 MUST NOT（不得）被列為未處理影片**，錨點亦得以越過。若比照未處理影片阻擋錨點，錨點將永遠停在最舊的未命中影片而每輪重複比對整個 Feed。

錨點的推進 MUST（必須）以上限方式計算，而非「取已處理影片中最新者」：候選 MUST（必須）限於發布時間**嚴格早於**所有未處理影片中最早者的影片，再取其中最新者。逕取已處理影片的最大發布時間 MUST NOT（不得）被視為滿足本需求 —— 當較新的影片未命中關鍵字、較舊的影片命中卻因狀態無法判定而未處理時，該作法會把錨點推過那支影片，使其下次檢查不再被評估。

本輪若沒有任何影片被處理，錨點的處置 MUST（必須）依原因區分：全數因**狀態無法判定**而未處理時，錨點 MUST（必須）維持不變；全數因未命中關鍵字、或因已判定為直播而未建立任務時，錨點 MUST（必須）依上述上限規則正常推進。

完整的候選視窗上限與精確發布時間守門規則見本能力的 `Periodic Check & New Video Matching`；本需求 MUST NOT（不得）與其相互抵觸。

#### Scenario: 最新影片因直播而被跳過

- **WHEN** 檢查取得的最新影片是**已判定為直播**而被跳過，其後另有一支較舊、已成功加入佇列的一般影片
- **THEN** 錨點 MUST（必須）依上限規則正常推進，MUST NOT（不得）因該直播影片而受限
- **AND** 下次檢查時該直播 MUST NOT（不得）再被視為待評估項目 —— 已知的直播內容一律不予下載

#### Scenario: 被跳過的直播結束後轉為存檔影片

- **WHEN** 先前因直播而被跳過的影片已結束直播、可正常下載，且其發布時間未曾改變
- **THEN** 由於錨點已越過它，系統 MUST NOT（不得）於後續檢查中將其判定為新片
- **AND** 此為刻意的取捨：為了保留該影片而壓住錨點，會使永遠存在排程直播的頻道錨點卡死，代價高於漏失該影片本身

#### Scenario: 本次所有影片皆被跳過

- **WHEN** 本次檢查取得的影片全數因**狀態無法判定**而被跳過（即全數為未處理影片，無任何影片是因未命中關鍵字或已判定為直播而略過）
- **THEN** 錨點維持不變，不做任何推進

#### Scenario: 本次所有影片皆因未命中關鍵字而未建立任務

- **WHEN** 本次檢查取得的影片全數符合既有新片條件，但全部未命中該頻道的關鍵字篩選，且本輪無任何未處理影片
- **THEN** 錨點 MUST（必須）依精確發布時間安全規則推進，MUST NOT（不得）因「沒有任何影片被處理」而維持不變
- **AND** 後續輪詢 MUST NOT（不得）再次比對這些已被越過的未命中影片

#### Scenario: 較新的未命中影片不得把錨點推過較舊的未處理影片

- **WHEN** 本輪影片依發布時間由新至舊為「未命中關鍵字的影片、命中但**狀態無法判定**的影片、已成功加入佇列的影片」
- **THEN** 錨點 MUST（必須）推進至最舊那支已加入佇列影片的發布時間
- **AND** 錨點 MUST NOT（不得）推進至那支未命中影片的發布時間，亦 MUST NOT（不得）越過中間那支狀態無法判定的影片

#### Scenario: 頻道永遠存在排程直播時錨點仍能推進

- **WHEN** 某頻道每日建立固定節目的排程直播，其候選視窗中因此**永遠**存在已判定為排程未開播的影片
- **THEN** 錨點 MUST（必須）能推進至本輪最新的已處理影片，MUST NOT（不得）被那些排程直播釘住
- **AND** 後續輪詢 MUST NOT（不得）將錨點之前的影片重複判定為新片
- **AND** 使用者清空下載佇列後再次檢查 MUST NOT（不得）使先前已處理過的影片重新入列
