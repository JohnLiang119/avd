## 1. Apply Layout 3 to Rust Server

- [x] 1.1 Update CSS in `src-tauri/src/server.rs` to include `.layout-3`, `.card-3`, flex column layout, and pill buttons.
- [x] 1.2 Update HTML generation loop in `src-tauri/src/server.rs` to use `<div class="card-3">` and match the structure of Android's `LocalFileServer.java`.
