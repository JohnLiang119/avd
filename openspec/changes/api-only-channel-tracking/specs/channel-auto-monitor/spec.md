## MODIFIED Requirements

### Requirement: Periodic Check & New Video Matching

系統 MUST 支援依使用者設定的間隔自動執行頻道輪詢，或由使用者手動觸發，透過 **YouTube Data API** 並以影片實際發布時間判定新影片 (`publishedTime > lastPublishedTime`)。輪詢間隔與「啟動時是否檢查」的可設定性見本能力的「檢查間隔與啟動時檢查須可設定」。

新影片的判定 MUST（必須）同時滿足下列三個條件：「發布時間晚於該頻道的時間錨點」、「尚未存在於下載佇列」，以及「影片標題命中該頻道的關鍵字篩選」。當該頻道未設定任何有效關鍵字時，第三個條件 MUST（必須）視為自動滿足，維持既有全量追蹤行為。頻道首次被追蹤時，系統 MUST（必須）僅初始化時間錨點而不建立任何下載任務。時間錨點 MUST NOT（不得）在缺少精確發布時間的情況下被推進至當下時間。

時間錨點的推進 MUST（必須）受下列上限守門。本輪若存在任何「未處理影片」（已命中關鍵字篩選，但因屬直播、首播或狀態未知而未建立下載任務者），錨點 MUST NOT（不得）晚於所有未處理影片中最早的發布時間；未命中關鍵字篩選的影片 MUST NOT（不得）被列為未處理影片，錨點得以越過。此外，當本輪取回影片中**最舊者的發布時間仍晚於該頻道目前的時間錨點**時，代表錨點與該最舊影片之間可能存在未被取回、亦未經比對的較舊影片，錨點 MUST NOT（不得）晚於本輪實際取回影片中最舊者的發布時間。反之，若本輪最舊影片已早於或等於目前錨點，代表本輪視窗已完整覆蓋「錨點至今」的缺口，不存在未經比對的較舊影片，此項上限 MUST NOT（不得）套用。

此上限 MUST NOT（不得）以「取回筆數是否達每輪的候選上限」作為判定條件。**筆數本身不代表視窗是否覆蓋錨點**：若每輪固定取回一定筆數且其時間跨度通常遠大於檢查間隔，以筆數判定會使上限恆成立，而該上限（本輪最舊者）早於現有錨點，致使錨點永遠無法推進、每輪重新比對整個清單。判定 MUST（必須）以視窗是否覆蓋錨點為準，與筆數無關。

頻道尚未建立時間錨點時（首次追蹤），此項上限 MUST NOT（不得）套用 —— 該情境不存在「錨點與最舊影片之間」的缺口，本就無從跳過任何影片。

#### Scenario: Auto and manual checking

- **WHEN** 距離上次檢查已達使用者設定的間隔，或使用者點擊手動檢查
- **THEN** 系統以 YouTube Data API 取得影片清單並比對實際發布時間篩選出新發布的影片，再以該頻道的關鍵字篩選決定哪些影片可建立下載任務
- **AND** 系統依上述錨點上限守門規則更新該頻道的時間錨點與最新已知影片識別，MUST NOT（不得）無條件將錨點設為本輪最新影片的發布時間

#### Scenario: First time channel subscription anchor

- **WHEN** 使用者剛新增追蹤頻道或頻道首次執行檢查（`lastPublishedTime` 與 `lastCheckTime` 皆無值）
- **THEN** 系統將該頻道目前最新影片的發布時間與 ID 設為起始基準錨點，不觸發歷史影片下載
- **AND** 若該最新影片不帶精確發布時間，系統不建立錨點，維持未初始化狀態待下次檢查
- **AND** 不論該最新影片是否命中該頻道的關鍵字篩選，首次錨點皆以本輪取回的最新影片建立

#### Scenario: 影片已存在於下載佇列

- **WHEN** 某支影片的發布時間晚於錨點，但佇列中已有相同影片 ID 的任務
- **THEN** 系統不重複建立任務
- **AND** 去重比對涵蓋扁平任務，以及頻道群組底下播放清單的巢狀子任務

#### Scenario: 取得精確發布時間時推進錨點

- **WHEN** 本次檢查有影片被實際處理，且其帶有精確發布時間
- **THEN** 系統將該頻道的時間錨點更新為已處理影片中最新者的發布時間
- **AND** 若本輪存在未處理影片，推進後的錨點 MUST NOT（不得）晚於所有未處理影片中最早的發布時間

#### Scenario: 缺少精確發布時間時保留錨點

- **WHEN** 檢查取得的最新影片不帶精確發布時間（例如來源未回傳可解析的時間欄位）
- **THEN** 系統保留該頻道原有的時間錨點不變
- **AND** 不以當下時間推進錨點，避免基準被推至未來而導致後續永久漏片

#### Scenario: 未命中關鍵字的影片可被錨點越過

- **WHEN** 本輪符合「發布時間晚於錨點」與「尚未存在於下載佇列」的影片中，有一支或多支未命中該頻道的關鍵字篩選，且其餘影片皆已處理完成
- **THEN** 系統 MUST 依既有精確發布時間安全規則推進錨點，未命中影片 MUST NOT（不得）成為錨點上限
- **AND** 後續輪詢 MUST NOT（不得）再次比對或重複處理已被錨點越過的未命中影片

#### Scenario: 未處理影片阻止較新未命中影片推過錨點

- **WHEN** 本輪同時出現「較舊、已命中關鍵字，但因屬直播、首播或狀態未知而未建立任務」的影片，以及「較新但未命中關鍵字」的影片
- **THEN** 系統 MUST 以該未處理影片的發布時間作為錨點上限，錨點 MUST NOT（不得）被較新的未命中影片推過該時間
- **AND** 下一輪檢查 MUST 仍能重新評估該未處理影片，並在其成為可下載內容時建立下載任務

#### Scenario: 候選視窗達上限時限制錨點推進

- **WHEN** 本輪取回影片中最舊者的發布時間仍晚於該頻道目前的時間錨點（例如該頻道自上次檢查以來已發布超過本輪取回筆數的影片）
- **THEN** 系統 MUST NOT（不得）將錨點推進至晚於本輪實際取回影片中最舊者的發布時間
- **AND** 系統 MUST NOT（不得）因本輪影片全部未命中關鍵字而越過本輪未被取回、亦未經比對的較舊影片

#### Scenario: 本輪視窗已回溯至錨點時不限制錨點推進

- **WHEN** 本輪取回影片中最舊者的發布時間已早於或等於該頻道目前的時間錨點（例如每輪 50 筆的視窗橫跨數日而檢查間隔僅數十分鐘）
- **THEN** 系統 MUST NOT（不得）套用候選視窗上限，錨點 MUST（必須）依其餘守門規則正常推進至本輪最新的已處理影片
- **AND** 系統 MUST NOT（不得）僅因取回筆數達每輪上限就限制推進 —— 那會使錨點永遠停滯並每輪重新比對整個清單

#### Scenario: 尚未建立錨點時不套用候選視窗上限

- **WHEN** 頻道首次被追蹤、尚未建立時間錨點
- **THEN** 系統 MUST NOT（不得）套用候選視窗上限
- **AND** 系統 MUST（必須）依既有規則僅初始化錨點而不建立任何下載任務

#### Scenario: Display latest publish time on channel card

- **WHEN** 使用者檢視已追蹤頻道清單
- **THEN** 系統在各頻道卡片上展示該頻道最新影片之發布時間格式化資訊

#### Scenario: Task title with publish time on main queue

- **WHEN** 系統將任何影片建立、進行下載或完成為下載任務並於主畫面佇列呈現（包含使用者手動單一加入、自動追蹤或播放清單解析）
- **THEN** 系統產生的任務標題（抬頭）在排隊、下載中與完成後始終穩定包含該影片之發布時間格式化資訊 (YYYY/MM/DD HH:mm:ss)，不因進度更新或下載完成事件而遺失

#### Scenario: 新影片以優先順序加入佇列

- **WHEN** 篩選出一支或多支同時滿足三項新片判定條件（含命中關鍵字篩選）的影片
- **THEN** 系統為每支通過關鍵字篩選的影片建立下載任務，任務標題包含頻道名稱前綴與發布時間
- **AND** 任務的子資料夾名稱以頻道名稱產生，且已移除檔案系統不接受的字元
- **AND** 系統 MUST NOT（不得）為未命中關鍵字篩選的影片建立下載任務

## REMOVED Requirements

### Requirement: Optional Fallback Mechanism and Source Transparency

**Reason**: 該需求規範的兩件事在單一通道下皆不再成立。「yt-dlp 備援開關」隨備援通道一併移除（見 `yt-dlp-rss-fallback` 的 REMOVED 說明）；「來源透明度」的前提是存在多個品質不同的來源需要讓使用者辨別，而頻道追蹤自此只有 YouTube Data API 一條通道，標註來源已無資訊量。

**Migration**: 頻道影片擷取一律經 API。任務狀態文字不再標註來源通道；使用者需要知道的不再是「這批資料來自哪裡」，而是「追蹤是否正常運作」—— 後者由 `youtube-data-api-channel` 的「API 不可用時須有持續可見的狀態指示」承接。既有的 `enableYtDlpFallback` 設定值於還原舊備份時 MUST（必須）被忽略，MUST NOT（不得）因該欄位存在而拒絕匯入。

## ADDED Requirements

### Requirement: 檢查間隔與啟動時檢查須可設定

系統 MUST（必須）讓使用者設定自動檢查的間隔，以及「應用程式啟動時是否立即補做一次檢查」。兩者 MUST（必須）持久化並於重啟後還原。

**間隔 MUST（必須）有依頻道數計算的安全下限。** 每輪的配額成本與啟用中的頻道數成正比，而每日配額為固定額度；使用者若在頻道數增加後仍沿用極短的間隔，會在無預警的情況下耗盡配額 —— 而移除備援後，配額耗盡等於追蹤完全停止。系統 MUST（必須）阻止使用者設定低於該下限的間隔，或於超出時明確警示，MUST NOT（不得）靜默接受一個必然耗盡配額的設定。

下限的計算 MUST（必須）保留餘裕，不得以剛好用滿每日配額為目標 —— 使用者可能於同一 Google Cloud 專案上有其他用途，且手動檢查與加入頻道亦消耗配額。

「啟動時是否檢查」的預設值 MUST（必須）維持既有行為（距上次檢查已達間隔時補做一次），使既有使用者升級後不感到行為改變。

#### Scenario: 設定間隔並於重啟後生效

- **WHEN** 使用者將檢查間隔改為某個合法值並重新啟動應用程式
- **THEN** 系統 MUST（必須）以該間隔執行後續的自動檢查

#### Scenario: 間隔低於安全下限時被阻止

- **WHEN** 使用者嘗試設定的間隔，會使目前頻道數的每日配額消耗超出安全額度
- **THEN** 系統 MUST NOT（不得）靜默接受該設定
- **AND** 系統 MUST（必須）告知該下限及其依據（頻道數與每日配額）

#### Scenario: 頻道數增加後下限隨之提高

- **WHEN** 使用者在設定短間隔後新增頻道，使既有間隔低於新的安全下限
- **THEN** 系統 MUST（必須）於頻道管理介面告知此情形
- **AND** 系統 MUST NOT（不得）在使用者未察覺的情況下持續以會耗盡配額的頻率檢查

#### Scenario: 關閉啟動時檢查

- **WHEN** 使用者關閉「啟動時檢查」並重新啟動應用程式，且距上次檢查已超過設定的間隔
- **THEN** 系統 MUST NOT（不得）於啟動時立即檢查
- **AND** 後續的定時檢查 MUST（必須）照常運作

#### Scenario: 啟動時檢查的預設值維持既有行為

- **WHEN** 既有使用者升級後未曾變更此設定
- **THEN** 系統 MUST（必須）沿用既有行為：距上次檢查已達間隔時，於啟動後補做一次檢查
